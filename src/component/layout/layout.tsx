import './layout.scss';
import MenuBlok from '../videoBlok/Menu';

const Layout = () => {
  return (
    <section className="layout-container">
      <MenuBlok />
    </section>
  );
}

export default Layout;
